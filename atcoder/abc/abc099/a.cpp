#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (int)(n); i++)
using namespace std;
typedef long long ll;
const ll INF = (ll)1 << 60;

int main() {
    int N;
    cin >> N;
    if(N < 1000) cout << "ABC" << endl;
    else cout << "ABD" << endl;

    return 0;
}