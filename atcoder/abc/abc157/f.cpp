#include <bits/stdc++.h>

using namespace std;
#define rep(i, n) for(int i = 0; i < (n); i++)

int main() {
  cin.tie(0);
  ios::sync_with_stdio(false);
  vector<int> a(3, 0);
  rep(i, a.size()){
    cout << a[i] << endl;
  }

  return 0;
}